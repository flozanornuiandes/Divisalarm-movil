// Top-level build file: declara los plugins usados por los subproyectos,
// sin aplicarlos aqui (cada modulo los aplica con su propio "apply true/false").
plugins {
    id("com.android.application") version "8.5.2" apply false
    id("org.jetbrains.kotlin.android") version "1.9.24" apply false
}
